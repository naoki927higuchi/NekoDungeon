# このプロジェクトの変更履歴

- ユーザーの方針：ローカルGitで変更履歴を管理し、まとまった変更が完了するたびに、追加の指示や確認なしでコミットする。
- 作業開始時に `git status` を確認し、既存の未コミット変更を勝手に破棄しない。
- 実装、関連ドキュメント、必要な検証を完了してから差分を確認し、変更内容が分かるメッセージでコミットする。
- Unityの `Assets`（`.meta`を含む）、`Packages`、`ProjectSettings`、ビルドスクリプト、作業ルールと説明書を管理する。
- `Library`、`Temp`、`Obj`、`Logs`、`UserSettings`、開発中のビルド成果物、ログはコミットしない。公開時に選定・検証した配布ZIPとSHA256・リリース情報のみDistributionへ置き、例外としてGit管理する。
- リモートへの送信・公開はユーザーからの明示的な指示がある場合のみ行う。
- 既存の履歴を書き換えず、原則として新しいコミットを追加する。過去の状態に戻す依頼では未コミット変更を保護する。
- 完了報告に保存したコミットの短いIDを記載する。コミットできなかった場合は、その理由を伝える。

## プロジェクト名と配布

- プロジェクト名・フォルダー名・GitHubリポジトリ名はNekoDungeon。ゲーム内の日本語タイトルは維持する。
- 開発中のZIPはBuilds/LocalPackagesへ保存し、ローカル管理のみとする。
- リモートへ送るときに選定したZIPだけをPrepare-Release.ps1でDistributionへコピーする。
- バージョンはSemantic Versioning。公開初版は1.0.0。上位を増やしたら下位は0に戻す。
- 公開バージョンの日時・概要をHISTORY.mdへ記録し、バージョン更新コミットを作成する。
- 公開済みZIPを同じ版数で上書きしない。過去の開発ZIPを一括でGit追加・pushしない。
- 今回の公開初版は既存の最新検証済みZIPをバイト単位で保持して採用する。再ビルドしない。
